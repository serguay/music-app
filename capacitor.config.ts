import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.serguay.musicapp',
  appName: 'com.serguay.musicapp',
  webDir: 'dist'
};

export default config;
